# Copy this file to .env and fill in your value.
# Google Sheet CSV source for the news section.
# Accepts a "Publish to web" CSV URL OR the normal sheet URL
# (the normal URL is converted to Google's CSV endpoint automatically).
NEWS_SHEET_CSV_URL=

# Google Sheet CSV source for the highlights section (same publishing steps as news).
HIGHLIGHTS_SHEET_CSV_URL=
